{
  "name": "legionio-desktop",
  "version": "0.1.0",
  "description": "LegionIO desktop wrapper — system tray, dashboard UI, and native notifications",
  "private": true,
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build",
    "lint": "echo 'No JS build step — frontend is plain HTML/CSS/JS'"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
